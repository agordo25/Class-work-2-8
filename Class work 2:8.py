***
#Design a function that prints the even numbers in a range of numbers
def even(n): 
    for number in range(1, n+1):
        if number %2 != 2:
            print(number, "is even")

even(100)

#function that returns the addition of 2 numbers
def sum_of_numbers(x,y,z):
    x=int("Enter a number")
    y=int("Enter a second number")
    z=int("Enter a third number")
    return x+y+z


import turtle as t
t,foward(50)
t.left(90)
t.foward(100)

import turtle as t
def drawRectnagle(t,w,h)
    for i in range(2):
        t.foward(w)
        t.left(90)
        t.forward(h)
        t.left(90)

drawRectangle(t,300,150)

def drawSquare(tx,sz):
    drawRectangle(tx,sz, sz)

drawSquare(t, 50)
